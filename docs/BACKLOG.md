# Backlog

Things deliberately deferred, plus ideas that surface mid-build. This is the parking lot — putting something here means "real, but not now." `/wrap-sprint` and `/close-sprint` may add items here when reviewers spot future work.

## Deferred by design (with a known path forward)

- **Playlists** with private/shared visibility (shared = visible within the library, never public). Epic 10+.
- **Search** by title and genre. Epic 10+. (Table Storage supports this; metadata model already carries `genre`.)
- **Admin experience**: filter deactivated (default off, toggle on), bulk hard-delete of deactivated songs. Epic 10+.
- **Soft-delete / deactivate lifecycle UI** (uploader deactivates own; admin deactivates any; admin hard-deletes). Data model already supports the states.
- **Database beyond Table Storage** — only if Table Storage is ever outgrown.
- **CDN** — only at many-listener scale; goes in front of Blob Storage, no re-architecture.
- **Multi-library** (formerly "multi-tenancy" before D19's rename) — `libraryId` is already threaded through every record (D19), and authorization is already library-scoped (D20), so the soft multi-library path is additive. When this lands, the `admin` role splits into a **library-scoped admin** (manages one library) and a **platform/global admin** (manages the app across libraries), and a library-membership concept is introduced (which users belong to which library, in which role) — see D20 for the deferred design.
- **Self-service public-provider login** for non-family listeners — Entra Model A covers the family now.
- **Android Play Store wrapper** (TWA around the PWA) — build PWA-shaped now, wrap later.
- **Separate-account backup** — soft-delete + versioning covers the start.
- **Service Bus** — only if Storage Queue is outgrown.
- **Per-kid private spaces** via guest-invite Entra accounts — when kids are old enough to want them.
- **Bring-your-own functions for "Managed Identity everywhere"** — the migration path away from D25's split auth posture. SWA *managed* functions can't use Managed Identity (platform limitation); bring-your-own functions can. The migration would eliminate the storage connection string secret entirely. Costs: requires SWA Standard tier per environment, plus a separately-managed Azure Functions resource. Triggered when the connection-string posture becomes painful (e.g., a secret rotation incident, or a desire for a uniform auth story across API + transcoder). Not now.
- **Custom domain for prod** (D26) — bumps prod SWA to Standard tier (~$9/mo) for a memorable family-facing URL. Explicitly Sprint 9.1's decision.

## Open questions to resolve at the right epic

- Opus vs AAC as the single output default — decide in Epic 6.
- ~~Keep the original upload after transcoding (for future re-transcode) or discard to save space — decide in Epic 5/6. (Lean: discard at family scale.)~~ **RESOLVED by D28: originals are kept indefinitely as the canonical archive; raw-uploads area is forbidden to delete from.**
- Exact input format allowlist + max upload size — decide in Epic 5.
- ~~Hard-delete retention window length on Blob soft-delete — decide in Epic 1.~~ **RESOLVED by Sprint 1.3:** 30 days for both blob soft-delete and container soft-delete on the dev storage account. Reasoning: aligns with the `table-backups` 30-day pruning window (D33), giving the project a single recovery-cushion number to remember. Family-scale cost difference vs. shorter retention windows is pennies/month (soft-deleted blobs are billed at their stored tier until the retention expires); soft-delete is the recovery cushion when a delete is issued via the API rather than a versioning-tracked overwrite. Prod (Sprint 9.1) re-evaluates independently — same number is the default if no specific reason to differ.
- `ownerDisplayName` freshness — cache once at upload time (stale-name risk on later display) vs. refresh from the latest token on every read (more API churn, plus a minor privacy implication if a user changes their handle and old records leak the old one) — decide in Epic 4.
- **Table Storage schema evolution policy** — additive-only by default; upgrade-on-read pattern for breaking changes. Pin in `docs/DEVELOPER_GUIDE.md` before Epic 4 starts (the first sprint with persisted records).
- **Shared-types deploy coordination across frontend/API (SWA) vs. transcoder (Container App)** — they deploy separately, so a breaking `Song` type change has a window where the two halves disagree. Pin the deploy-order discipline in `docs/DEVELOPER_GUIDE.md` before Epic 6.
- **API error response shape** — pin a single shape (e.g. `{error: {code, message}}` + correlation id, appropriate HTTP status) so endpoints don't drift. Decide in Sprint 4.2.
- **Upload idempotency mechanism** — client-generated idempotency key OR client-generated song id treated as upsert, so a retry after a dropped network does not create a duplicate song. Decide in Sprint 5.2.
- **Audio cache invalidation strategy (PWA / browser cache)** — key on `blobPath + etag/version`, not on `songId` alone, so deactivation and re-transcode invalidate cleanly. Decide in Sprint 8.2.
- **Secret rotation policy** — exact rotation cadence + step-by-step procedure for the storage connection string, SWA deploy token, and App Insights connection string. One-liner is enough; lands in `docs/DEVELOPER_GUIDE.md` "Secrets" section whenever convenient (no sprint dependency).
- ~~**`/api/health` response minimization** — `{status: "ok"}` and a 200, no version/build/environment info. Decide and pin in Sprint 0.3 (frontend health call) / Sprint 2.2 (deployed health) planning.~~ **RESOLVED by Sprint 0.3**: implemented as `{status: "ok"}` in `api/src/functions/health.ts`; the no-version-leak invariant is enforced by a test in `api/src/functions/health.test.ts` (LOW: severity if a future change adds `version`/`build`/`env` fields, the test catches it).

## Emergent (added during build)

- **Wire App.tsx fetch-error console.error to App Insights as structured telemetry (Epic 1+).** Currently the App.tsx fetch catch logs to `console.error` so devtools shows the failure cause during dev. When App Insights lands (D24, Sprint 1.1), replace with a structured error event (no PII per D32; the health check has no request body so this is just the failure shape). Source: Sprint 0.3 `/wrap-sprint` Finding F4 (LOW).
- **Real mobile-first UI (Epic 8).** Sprint 0.3 added the minimum: `<meta viewport>` is set, the footer link has WCAG 2.2 ≥44×44 touch-target padding, system fonts, max-width 40rem. This is the bridge, not the design. Epic 8 builds the real responsive layout, the player UI, brand styling, etc. Source: Sprint 0.3 `/wrap-sprint` Finding F5 (LOW).
- **Re-evaluate happy-dom vs jsdom (Epic 8).** Frontend tests currently use happy-dom (D23 implementation note — chosen for speed + small footprint, sufficient for skeleton tests). If Sprint 8's real UI hits a DOM API happy-dom doesn't implement (SVG corners, some CSSOM features, edge cases of form validation), flip to jsdom — costs a re-run of every then-existing frontend test against the new environment. Catch it the moment a test failure looks DOM-emulator-related. Source: Sprint 0.3 `/wrap-sprint` Finding F3 sub-decision.
- **Additional API handler unit tests as endpoints land (Sprint 4.2 / 5.2 / 6.1+).** Sprint 0.3 added `api/src/functions/health.test.ts` (2 tests — response shape + no-version-leak invariant). The pattern is: pure-function unit test of the handler, no func host needed (the `@azure/functions` package self-detects test mode and skips registration). Use this as the template for `/api/songs`, `/api/upload`, transcoder handlers, etc. Source: Sprint 0.3 `/wrap-sprint` Finding F3.
- **Dedicated alerts mailbox (revisit when prod lands, Sprint 9.1 or sooner).** Sprint 1.1's dev budget alert and any future Azure / GitHub Actions / App Insights notifications currently target the maintainer's personal MSA email (set in Azure budget config; intentionally NOT in any committed file per public-repo hygiene). For a personal family project this is fine — but a dedicated alias (e.g., a separate gmail like `slaylist.alerts@gmail.com`, or a forwarding alias on a domain) gives three benefits: (a) cleaner separation of "personal" vs. "project" noise; (b) easier handoff if anyone else ever needs alerts (sharing an alias is safer than sharing a personal account); (c) protects the personal email from any leak via Azure portal screenshots, support tickets, etc. Implementation when triggered: create the alias, update every Azure notification target (budget alert, App Insights smart-detect alerts, deploy failure alerts), update the gitignored `dev-resources.md` + `prod-resources.md` "Notification email" field — no committed file changes. Trigger options: (1) before Epic 9 / Sprint 9.1 so prod is born with the right address, (2) earlier if a real notification volume builds up. Source: Sprint 1.1 owner-raised concern about personal email in source code.
- **Prod budget alert — mirror of dev's $5/$12/$15, recalibrated against real prod baseline (Sprint 9.1).** Sprint 1.1 created a dev RG-scoped budget at $5/$12/$15 on `rg-music-slaylist-dev-use2`. The matching prod budget on `rg-music-slaylist-prod-use2` is deliberately deferred to Sprint 9.1 — creating it now would watch an empty resource group and produce no signal. At Sprint 9.1, mirror the three-rung pattern but recalibrate the dollar amounts against prod's actual expected baseline (which will be higher than dev because real traffic + real storage + Container App execution exists). Per `docs/DECISIONS.md` D24 the budget alert is cost observability separate from failure observability; D7 keeps the two environments physically separate including their cost watches. Source: Sprint 1.1 plan, owner-approved deferral.
- **Storage diagnostic logs will contain title-slug PII once real uploads land — D8 ↔ D32 tension to resolve before Sprint 5+ (revisit at Sprint 5.2 filename-convention decision; Sprint 4.1 also relevant when log statements first ship).** Surfaced at Sprint 1.3 /wrap-sprint Finding F2 (MODERATE). The tension: per D8, blob filenames are `{title-slug}_{timestamp}_{shortid}.{ext}` — title-slug is the human-readable form (chosen for restore readability). Per D32, *title is PII* (kids' songs are often named after kids or family members). The diagnostic settings configured at Sprint 1.3 Step 8 capture `StorageBlobLogs` which include a Uri field. Once real uploads happen at Sprint 5+, `StorageBlobLogs.Uri` values WILL contain title-slug fragments (PII) and land in the LAW. At Sprint 1.3 there's no real content yet — this is the early-warning to address before Sprint 5+ ships real uploads. **Three mitigation options for Sprint 5.2 to choose between:** **(a)** Restrict diagnostic-logging scope at the storage account level to exclude Uri/path fields (verify Azure supports this granularity — it may only allow whole-category on/off, not per-field). **(b)** Extend D32's songId-based-local-filename mitigation upstream to blob storage — i.e., blob filename = `{shortid}_{timestamp}.{ext}` with the title stored in the blob's metadata properties only, accepting that "restore-readability" via D8 becomes "restore-via-metadata-lookup." **(c)** Accept the LAW as PII-bearing (it already is per D32 for App Insights data) and apply the same RBAC + retention discipline to StorageBlobLogs; treat all LAW data as PII-bearing uniformly. **Recommended posture**: option (b) — eliminates the leak channel at the source rather than filtering. Option (c) is the path-of-least-resistance fallback if (a) and (b) prove impractical. The choice converges with Sprint 5.2's filename-convention decision (which is the natural moment to lock the slug-vs-shortid pattern in storage). **Critical**: do NOT ship any real upload to Sprint 5+ without resolving this. /wrap-sprint InfoSec adds a check at Sprint 5.x that StorageBlobLogs are queried + no title-slug leak observed in the Uri field. Source: Sprint 1.3 /wrap-sprint Finding F2 (MODERATE).
- **Smoke-test PowerShell URL construction brittleness (Sprint 9.1 prod equivalent + any AI smoke-test reuse).** The Sprint 1.1 portal guide's Step 5 Option B builds the ingestion URL by concatenating `$endpoint = "<your-ingestion-endpoint>v2/track"` — relying on the AI connection-string convention of always ending `IngestionEndpoint=` with a trailing slash. Documented behavior, has held forever, but is a one-character brittleness: if Microsoft ever returned the endpoint without trailing slash, the URL would become `...comv2/track` and the smoke test would fail opaquely with a DNS error. Defense: switch to `($endpoint.TrimEnd('/') + '/v2/track')` in the construction. Low priority — at family scale we'd notice the broken smoke test in the same 5 minutes we ran it. Pick up when touching the guide for Sprint 9.1 or any later AI smoke-test reuse. Source: Sprint 1.1 /wrap-sprint Finding F5 (LOW).
- **Residual dev-only Dependabot alerts from Azure dev tooling (revisit at Epic 1.x and Epic 2.x).** Sprint 0.4 follow-up bumped happy-dom 15→20 (resolved 20.9.0) to clear three CVEs (CVE-2025-61927 Critical RCE + CVE-2026-34226 + CVE-2026-33943). The residual alerts (axios SSRF/credential-leak family, `@azure/identity` / `@azure/msal-node` transitive ranges) are reachable only through `azurite`, `@azure/static-web-apps-cli`, and `azure-functions-core-tools` — all **dev-only** devDependencies that never ship to any deployed environment and never see production data per D7. All three are already at their latest npm versions (azurite 3.35.0, @azure/static-web-apps-cli 2.0.9, azure-functions-core-tools 4.11.0) as of 2026-05-24, so the fix is **gated on upstream releases**, not on us. **Revisit triggers:** (a) before Epic 1.1 wires App Insights — re-run `pnpm audit` and `npm view` on the three dev tools to see if updated transitives shipped; (b) before Epic 2.2's first deploy workflow — same check, with extra attention to anything `@azure/static-web-apps-cli` pulls during deploy; (c) any Dependabot alert that escalates to Critical on a path reachable at runtime (vs. dev-only). The current LOW-risk read holds **only while these remain dev-only**; if a later sprint ever ships any of these in a deployed runtime, re-triage immediately. Source: Sprint 0.4 follow-up Dependabot triage (rayklundt InfoSec consult).
