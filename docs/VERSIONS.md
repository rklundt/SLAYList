# Versions

Tracks notable versions/milestones of the project and key dependency/tooling versions. Updated by `/close-sprint` when a sprint changes something version-worthy.

## Project milestones

| Version | Date | Milestone |
|---|---|---|
| 0.0.1 | (scaffold) | Initial project scaffold: docs, sprint structure, command definitions. No code yet. |
| 0.0.2 | 2026-05-24 | Pre-Epic-0 alignment: AGPL-3.0-or-later licensing (D15), DCO + relicensing grant (D16), public repo from commit one (D17), Node 22 LTS + npm workspaces pinned (D18). |
| 0.0.3 | 2026-05-24 | Sprint 0.1 complete: public GitHub repo at `rklundt/SLAYList`; `develop` + `main` with branch protection; D31 baseline 3/4 (secret scanning, push protection, Dependabot security updates) — CodeQL deferred to Sprint 0.2 retry because GitHub's default-setup requires detected source languages and the initial scaffold is markdown-only. |
| 0.0.4 | 2026-05-24 | Sprint 0.2 complete: monorepo scaffold (pnpm 11.3.0 workspaces per D34, supersedes D18's npm pin; working tree moved off exFAT to an NTFS path due to pnpm's symlink requirement — see D34 implementation note); `/shared` types module per D19/D21 with locked `SongState`/`Role`/`DEFAULT_LIBRARY_ID`; Vitest 3.2.4 + TypeScript 5.9.3 + `@types/node` 22.19.19 with strict + noUncheckedIndexedAccess (D29) clean across all 4 workspaces; 5 invariant tests passing. CodeQL retry (D31) still pending — actually fires post-merge of this sprint's PR, when TS code lands on default branch. New public-repo hygiene rule: no personal-machine local paths in committed files or commit messages. |
| 0.0.5 | 2026-05-24 | Sprint 0.2 post-merge: CodeQL default-setup configured against the now-TypeScript-containing develop branch (D31 baseline 4/4 complete). Correct HTTP verb confirmed as `PATCH`, not `PUT` — both Sprint 0.1 and Sprint 0.2 retry attempts had used PUT and 404'd for compounded reasons (Sprint 0.1: no language to detect; Sprint 0.2: wrong verb). Documented in D31's resolution note + Sprint 0.2 SPRINTS.md acceptance line. |
| 0.0.6 | 2026-05-24 | Sprint 0.3 complete: local run with one command. Vite 6.4.2 + React 19 frontend on port 5193 with "SLAYList — it works" page + AGPL §13 footer + green/red round-trip indicator. Azure Functions v4 programmatic model API on port 7071 (`/api/health` returning `{status:"ok"}`). Four-process `pnpm dev` stack via concurrently (storage/Azurite + tsc-watch + func + Vite), with predev hook for initial API build. New: D35 (Azurite required for local Azure Functions dev). D23 implementation note added (happy-dom over jsdom for frontend test environment). 12 tests passing (5 shared + 2 api + 5 frontend) — Vitest CI gate still soft per D23 until Sprint 4.1 hardens. `scripts/dev.sh` (tracked, portable) plus optional gitignored `/dev.sh` for per-machine overrides. 4 BACKLOG Emergent entries recording deferrals from `/wrap-sprint` findings. |
| 0.0.7 | 2026-05-25 | Sprint 1.1 complete: dev Azure landing zone provisioned by hand. `rg-music-slaylist-dev-use2` (East US 2) with the five-tag set (`managed-by=manual`); workspace-based App Insights (`appi-music-slaylist-dev-use2`) backed by Log Analytics workspace (`log-music-slaylist-dev-use2`, 30-day LAW default retention); RG-scoped $15/mo budget (`budget-music-slaylist-dev`) at $5/$12/$15 Actual-cost rungs with direct-email notification (no action group). Failure-observability sink (D24) verified end-to-end before any real code exists — a PowerShell POST of a `smoke-test-1.1` custom event landed in `customEvents` within ~2 min via the ingestion endpoint and was queryable through the Logs blade. New: **D36** (App Insights stays on public ingestion + public query; Private Endpoints deferred until a real trigger). Tag-convention adjustment: `managed-by=manual` (no sprint suffix; provenance recoverable from git history) — supersedes the original `manual-<sprint>` form in `infra/naming-convention.md`. `/wrap-sprint` was the second pass after Phase B (owner initially called the skip; reversed when proposing the close-sprint doc batch). |
| 0.0.9 | 2026-05-27 | Sprint 1.3 complete: dev storage account provisioned by hand. `stmusicslaylistdevuse2` (East US 2, StorageV2 Standard LRS, Hot tier, Hierarchical Namespace OFF) with three private blob containers (`raw-uploads`, `finished`, `table-backups`) and the empty `Songs` table. 30-day blob + container soft-delete + blob versioning enabled. **D28 invariant verified empty at creation** (zero deletion-class lifecycle rules on `raw-uploads`). Diagnostic settings `diag-blob-to-law-dev` + `diag-table-to-law-dev` forward StorageRead/Write/Delete + Transaction metrics to `log-music-slaylist-dev-use2` (D24 third feeder leg) — **verified end-to-end** via test blob upload + KQL query returning rows in ~1 min (faster than 5-15 min worst-case ingestion). Connection string captured to gitignored `infra/dev-resources.md` as the SWA-managed-functions auth credential (D25); Sprint 2.1 stores as GitHub Actions secret `STORAGE_CONNECTION_STRING_DEV`; Sprint 2.2 wires into SWA app settings as `AzureWebJobsStorage`. New: **D38** (storage public network access stays enabled, extending D36's logic to the storage-specific case; promoted from /wrap-sprint Finding F1). BACKLOG open question *"Hard-delete retention window length on Blob soft-delete — decide in Epic 1"* RESOLVED with 30 days (matches the `table-backups` 30-day window per D33 for a single recovery-cushion number). BACKLOG new emergent entry capturing **D8 ↔ D32 storage-log PII tension** — must resolve before Sprint 5+ ships real uploads; three mitigation options enumerated; revisit triggers at Sprint 5.2 (filename convention) and Sprint 4.1 (log statements). |
| 0.0.8 | 2026-05-25 | Sprint 1.2 complete: dev Static Web App provisioned by hand. `swa-music-slaylist-dev-use2` (East US 2, **Free** tier per D26, **Deployment source = `Other`** — D22 safeguard against auto-deploy of pre-gate `develop` content; verified post-creation that no auto-workflow file landed in `.github/workflows/` and the SWA hostname serves Azure's default placeholder, not SLAYList content). Deployment token captured to gitignored `infra/dev-resources.md` as a break-glass fallback only — **NOT** in GitHub Secrets, **NOT** referenced by CI. New: **D37** (SWA deploy auth: OIDC-primary via `az staticwebapp deploy`, deployment token as gitignored break-glass fallback; extends D30 to the SWA-specific case). Sprint 2.1/2.2 acceptance updated to remove the SWA-deployment-token-as-CI-secret pattern and add load-bearing verification at 2.2 of the OIDC deploy path against the actual dev SWA (verify-don't-assume per D18/D27/D34/D36 discipline). D25 tightened to make explicit that Private Endpoint unavailability follows the same shared-platform constraint as Managed Identity unavailability on SWA managed functions (the foundation D36 rests on). Bonus tracked outcome: agent-tool branding scrubbed from committed prose + commit-message metadata (new "no agent-tool branding in committed prose or commit-message metadata" rule added to `docs/DEVELOPER_GUIDE.md` "Public-repo hygiene"; phase tag `[Claude Code]` → `[Agent]` across all SPRINTS.md files; D14 title renamed to "Human merges; the agent prepares"; `Co-Authored-By` trailers retired going forward — applied incrementally during Sprint 1.2's branding-scrub work). Post-merge follow-up: develop's history was rewritten in place to strip `Co-Authored-By: Claude` trailers from the PR #8 and PR #9 squash-merge commits, closing the "Claude as contributor" loop on the public GitHub Contributors view; same trees, only commit-message trailers changed; merged sprint branches (`sprint/1.1-rg-budget-appinsights`, `sprint/1.2-dev-static-web-app`) deleted from origin as cleanup. |

## Key tooling / dependency versions

| Item | Version | Notes |
|---|---|---|
| Node.js | **22 LTS** | Pinned across frontend, API, transcoder. Set by API constraint (D18). |
| Package manager | **pnpm 11.3.0** (D34, supersedes D18's npm pin) | Pinned via `package.json` `packageManager` field (corepack-compatible). CI uses `pnpm install --frozen-lockfile`. Working tree MUST be on a filesystem with symlink support (NTFS on Windows, ext4/APFS on Linux/macOS — exFAT/FAT32 break pnpm; see D34 implementation note). |
| Monorepo strategy | **pnpm workspaces** (D34) | `pnpm-workspace.yaml` lists `shared`, `api`, `frontend`, `transcoder`. Internal deps use the `workspace:*` protocol. pnpm 11 settings (e.g., `allowBuilds`) live in `pnpm-workspace.yaml`, not in `package.json`. |
| Frontend framework | React **19** + Vite **6.4.2** (TS) | Pinned in Sprint 0.3. Range `^19.0.0` (react/react-dom), `^6.0.0` (vite) in `package.json`; exact in `pnpm-lock.yaml`. Vite dev server on port **5193** (non-default to avoid 5173 conflicts with other local apps); production build emits to `frontend/dist/`. |
| API runtime (prod) | Azure Static Web Apps managed functions (Azure Functions v4, Node 22) | Set `apiRuntime: "node:22"` in `staticwebapp.config.json`. |
| API framework | **`@azure/functions` v4** (programmatic model) | Pinned in Sprint 0.3. `app.http(...)` registration pattern; no `function.json` files. Confirmed supported on SWA managed functions per `learn.microsoft.com/en-us/azure/static-web-apps/add-api` (verified 2026-05-24; last updated 2026-01-23). Range `^4.5.0` in `api/package.json`; exact in `pnpm-lock.yaml`. |
| API runtime (local dev) | **Azure Functions Core Tools 4.x (`func`)** | Microsoft's local runtime emulator for Azure Functions; what `pnpm dev` runs to host the API on port 7071. Install via [Microsoft installer](https://learn.microsoft.com/azure/azure-functions/functions-run-local) (Windows MSI) OR `npm install -g azure-functions-core-tools@4`. Not committed to the repo (per-machine binary install). |
| Local Storage emulator | **Azurite 3.35.0** (D35) | Microsoft's official Azure Storage emulator (blob/queue/table). Pinned in Sprint 0.3 as a root devDep. Runs as part of `pnpm dev` (port 10000/10001/10002, silent) to satisfy `func`'s `AzureWebJobsStorage` health check — without it the func host exits as unhealthy and the dev stack crashes (D35 documents the trap + alternatives considered). Data dir: `.azurite/` (gitignored). Will be the local stand-in for the real Azure Storage account when Epic 4+ adds blob/table code. |
| Dev process orchestrator | **`concurrently` 9.2.1** | Pinned in Sprint 0.3. Runs azurite + func + vite together for the `pnpm dev` one-command. `--kill-others-on-fail` so a crashed component takes the others down, surfacing failures immediately. |
| SWA local emulator (NOT used by `pnpm dev`) | **`@azure/static-web-apps-cli` 2.0.9** | Kept as a root devDep for Sprint 2.2's deploy-pipeline verification (the load-bearing pnpm+SWA Oryx build check). NOT used for local dev — its `swa start` has a hardcoded `require()` path that fails under pnpm's strict node_modules layout. See DEVELOPER_GUIDE "Why this dev pattern, not SWA CLI's `swa start`?" for the full reasoning. |
| Transcoder | ffmpeg in a Docker image | Base image + ffmpeg version pinned in Epic 6. |
| Testing framework | **Vitest 3.2.4** (D23) | Pinned from Sprint 0.2 (range `^3.0.0` in `package.json`; exact patch in `pnpm-lock.yaml`). CI gate is **soft** during Epics 0–3 (advisory, runs but does not block merge) and **hard** from Sprint 4.1 onward (failing tests block merge to `develop`). The flip is an explicit Sprint 4.1 acceptance criterion — not memory-dependent. |
| Frontend DOM emulator (test env) | **happy-dom** (D23 implementation note) | Pinned in Sprint 0.3 (`^15.0.0` in `frontend/package.json`). Sufficient for skeleton tests; reconsider trigger documented in D23 note (flip to jsdom if Sprint 8's real UI hits a happy-dom-unsupported API). |
| Component-test library (frontend) | **`@testing-library/react`** | Pinned in Sprint 0.3 (`^16.1.0` in `frontend/package.json`). Standard modern React testing pattern; pairs with happy-dom for the Vitest config above. |
| TypeScript | **5.9.3** with `strict: true` + `noUncheckedIndexedAccess: true` (D29) | Pinned from Sprint 0.2 (range `^5.7.0` in `package.json`; exact patch in `pnpm-lock.yaml`). Root `tsconfig.json` carries the strict settings; all workspaces extend the root. No workspace silently overrides these flags downward. |
| `@types/node` | **22.19.19** | Pinned to the 22.x line to match Node 22 runtime (D18). Range `^22.10.0` in `package.json`; exact patch in `pnpm-lock.yaml`. |
| Azure deploy auth | **OIDC federation** (D30) | Federated Entra app registration; no long-lived client secret stored in GitHub Secrets. Federated credentials keyed to repo + branch. Set up at Sprint 2.1. |
| GitHub repo security | **Secret scanning + push protection + Dependabot security updates + CodeQL** (D31) | All enabled at Sprint 0.1. Free on public repos. Push protection is the critical control. |
| CI provider | **GitHub Actions** | Workflows live in `.github/workflows/`. Chosen by Epic 2's pipeline. No other CI provider in scope. |
| Observability sink | **Application Insights** (D24) | Single failure-observability sink per environment. Created in Sprint 1.1 (dev) and Sprint 9.1 (prod). Fed by SWA managed functions, the Container App transcoder, and Storage diagnostics. Cost-observability (budget alert) is a separate concern with its own Epic 1.1 / 9.1 acceptance. |
| SWA tier (dev) | **Free** (D26) | Sufficient for the architecture as designed; Standard is not needed yet. |
| SWA tier (prod) | **Decided in Sprint 9.1** (D26) | Free or Standard; Standard only if custom domain is wanted. Documented known future fork. |
| Budget alert | **$15/month (dev)** | Generous enough not to false-alarm; low enough that anything runaway trips it. Prod alert level decided in Sprint 9.1. |
| Azure resource API/SKU choices | TBD | Filled in Epic 1. |

## Verified runtime support — Static Web Apps managed functions

Node 22 GA support for SWA managed functions verified **2026-05-24** against:

- Primary (authoritative): <https://learn.microsoft.com/en-us/azure/static-web-apps/languages-runtimes> — page last updated 2026-02-25. The Node 22 row in the API runtime table reads: *Node.js 22.x | Linux | Azure Functions 4.x | `apiRuntime` value `node:22` | no end-of-support date*. **This is the SWA-integrated-Functions-specific check** — the `apiRuntime` column header is exactly the field you set in `staticwebapp.config.json` → `platform.apiRuntime`, so `"node:22"` is the verbatim valid value, not an inference from "Node 22 is LTS." This addresses the long-standing concern that SWA's integrated Functions runtime can lag the latest LTS — for Node 22, it does not.
- Cross-check: <https://learn.microsoft.com/en-us/azure/azure-functions/functions-versions> — page last updated 2026-04-17. Lists Node 22 as GA on Functions v4, expected EoS 2027-04-30.
- Stale page caveat: <https://learn.microsoft.com/en-us/azure/static-web-apps/apis-functions> — the "Constraints" table on this page still lists Node 12/14/16/18/20-preview and is internally inconsistent with the languages-runtimes page. The languages-runtimes page is authoritative.

Re-verify these links when bumping Node major or before Epic 2 if more than a quarter has passed.

## How to update

`/close-sprint` proposes additions here when a sprint pins a version, adds a dependency, or hits a project milestone. Keep it terse — this is a ledger, not a changelog narrative (narrative lives in PR descriptions).
